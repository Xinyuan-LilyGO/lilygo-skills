# Tool: Espressif Documentation MCP

Use for ESP-IDF docs lookup, chip documentation, migration guidance, and official source lookup prompts.

- Primary authority: `https://mcp.espressif.com/`.
- Treat docs lookup as source authority, not build or flash evidence.
- Pair with `fw-esp-idf` or `fw-arduino` when framework behavior is involved.
