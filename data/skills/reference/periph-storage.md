# Peripheral: Storage

Use for SD, TF, SPIFFS, LittleFS, partition, asset, and filesystem prompts.

- Check bus pins, mount options, partition tables, image assets, and runtime logs.
- OTA prompts should trace to `app-ota` and `debug-flash-serial`.
- Do not claim media or filesystem access without command evidence.
