# Debug: LVGL Loop

Use for LVGL blank screen, touch input, rotation, refresh, heap, and timing prompts.

- Check tick source, draw buffer, flush completion, invalidation, input read callback, rotation, color order, heap, and task timing.
- Pair with `fw-lvgl`, `periph-display`, and a board skill.
- Report simulator or hardware boundary until evidence exists.
