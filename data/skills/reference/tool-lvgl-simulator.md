# Tool: LVGL Simulator

Use for LVGL PC simulator, page-data, screenshot, widget tree, and V4 render evidence prompts.

- Primary authority: `https://lvgl.io/docs/open/8.3/get-started/platforms/pc-simulator`.
- Pair with `fw-lvgl` and `debug-lvgl-loop`.
- Render success requires simulator output, page-data, screenshot, or board evidence.
