# Peripheral: Input

Use for touch, buttons, keyboard, encoder, and input event prompts.

- Pair touch prompts with `periph-display` and `debug-lvgl-loop`.
- Pair keyboard prompts with T-Deck source references.
- Verify input with event logs, page-data, simulator output, or board evidence before claiming success.
