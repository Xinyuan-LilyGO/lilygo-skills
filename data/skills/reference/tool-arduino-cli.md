# Tool: Arduino CLI

Use for Arduino CLI install, board manager, library manager, compile, upload, and monitor prompts.

- Primary authority: `https://docs.arduino.cc/arduino-cli/`.
- Keep commands reproducible: `arduino-cli core install`, `board list`, `compile`, `upload`, and serial monitor.
- Upload success requires captured command output and port evidence.
