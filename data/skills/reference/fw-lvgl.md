# Framework: LVGL

Use for LVGL display-loop prompts.

- Pair with `periph-display`.
- Check draw buffer sizing, flush callback, tick timing, rotation, color order, and backlight.
- This is LVGL context only until simulator output or real display evidence proves rendering.
