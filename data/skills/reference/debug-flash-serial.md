# Debug: Flash and Serial

Use for upload, flash, serial monitor, and debugging prompts.

- Keep commands reproducible through CLI tools such as `idf.py`, Arduino CLI, PlatformIO, espflash, or serial monitor tools.
- Capture exact port, baud, chip target, command, exit code, and serial excerpt when moving beyond context injection.
- This context routes build, flash, and serial guidance but does not verify real
  device communication by itself.
