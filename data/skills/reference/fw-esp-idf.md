# Framework: ESP-IDF

Use for ESP-IDF and `idf.py` prompts.

- Installation context should cover ESP-IDF export, target selection, build, flash, and monitor.
- Display/LVGL prompts need component, panel, PSRAM, and flush-loop context.
- Route output does not prove a real `idf.py flash` happened.
